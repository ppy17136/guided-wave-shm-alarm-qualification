# Release notes: v1.0.4

## Clean public release

This release is a documentation and public-release hygiene update. It aligns the repository with the final article framing, limits the reviewer-facing package to scientific reproducibility evidence, and removes an unnecessary temporary download workaround.

The update also synchronizes the worst-temperature sensitivity wording across the supplement-facing derived table and its builder, and qualifies byte-identical figure rebuilding as a pinned-environment result.

No score, threshold, denominator, gate criterion, figure content, author metadata, or frozen scientific outcome changed. The external COPV result remains 7/13 gates passed with strict outcome `FAIL`. Historical v1.0.1-v1.0.3 releases remain available through their immutable tags and archived records.
